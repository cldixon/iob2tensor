# the following checkpoints have been tested
# other checkpoints not specified here could
# likely be used as well. See `tests/test_tokenizers.py`.

SUPPORTED_TOKENIZERS = [
    "bert-base-cased",
    "bert-base-uncased",
    "bert-large-cased",
    "bert-large-uncased",
    "distilbert-base-cased",
    "distilbert-base-uncased",
    "roberta-base",
    "roberta-large",
]
